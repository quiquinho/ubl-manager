:80 {
    handle /api/* {
        reverse_proxy ublmanager-api:4000
    }

    handle {
        root * /usr/share/caddy
        try_files {path} /index.html
        file_server
        encode gzip
    }
}